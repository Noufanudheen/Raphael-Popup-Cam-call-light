async function load() { const r = await fetch('/api/status'); const j = await r.json(); const c = j.config; document.getElementById('enabled').checked = c.enabled; document.getElementById('rule').value = c.rule; document.getElementById('speed_ms').value = c.speed_ms; document.getElementById('pattern').value = c.pattern; } async function save(e) { e.preventDefault(); const payload = { enabled: document.getElementById('enabled').checked, rule: document.getElementById('rule').value, speed_ms: parseInt(document.getElementById('speed_ms').value), pattern: document.getElementById('pattern').value }; const r = await fetch('/api/config',{method:'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(payload)}); const j = await r.json(); document.getElementById('status').innerText = 'Saved'; } document.getElementById('config').addEventListener('submit', save); load();